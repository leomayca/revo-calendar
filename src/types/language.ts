export type SupportedLang = "en" | "es" | "de" | "pt" | "fr" | "ru";

export interface LanguageTranslations {
  days: string[];
  daysShort: string[];
  daysMin: string[];
  months: string[];
  monthsShort: string[];
  noEventForThisDay: string;
  allDay: string;
  addEvent: string;
  delete: string;
  eventTime: string;
  previousYear: string;
  nextYear: string;
  toggleSidebar: string;
  toggleDetails: string;
}
